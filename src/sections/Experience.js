import react from 'react';

import expclass from './Experience.module.css';

const Experience = (props) => {
  return (
    <section id="experience" className={expclass.body}>
      <p>These are my Experiences</p>
    </section>
  );
}

export default Experience;