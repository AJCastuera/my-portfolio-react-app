import react from 'react';

import skillclass from './Skills.module.css';

const Skills = (props) => {
  return (
    <section id="skills" className={skillclass.body}>
      <p>These are my skills</p>
    </section>
  );
}

export default Skills;